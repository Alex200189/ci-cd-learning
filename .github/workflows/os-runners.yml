name: Несколько раннеров (Ubuntu + Windows)

on:
  push:

jobs:
  ubuntu-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Работаю на ubuntu"
      - run: uname -a

  windows-job:
    runs-on: windows-latest
    steps:
      - run: echo "Работаю на windows"
      - run: systeminfo
