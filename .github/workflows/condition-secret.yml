name: Condition с секретом

on:
  push:

jobs:
  check:
    runs-on: ubunti-latest
    steps:
      - run: echo "секрет есть"
        if: '${{ secrets.MY_TOKEN != '' }}'
  deploy:
    runs-on: ubuntu-latest
    needs: check
    steps:
      - run: echo "деплой..."
